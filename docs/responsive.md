# Responsive strategy

The layout starts with a spacious desktop composition and collapses to a single-column flow below 700px.

Key rules:
- Keep hero typography fluid with `clamp()`.
- Let project artwork scale with viewport width on small screens.
- Hide secondary desktop navigation on narrow screens.
- Keep contact actions reachable without horizontal scrolling.
