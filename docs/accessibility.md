# Accessibility notes

- Use one clear `h1` on the page.
- Navigation uses a labeled `nav` landmark.
- Interactive controls use buttons or links rather than clickable generic elements.
- Project artwork has descriptive labels where it conveys information.
- Theme controls expose their state through ARIA attributes.
- Keep keyboard focus visible when extending the stylesheet.
