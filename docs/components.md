# Component map

The UI uses a small set of reusable visual patterns:

- `.site-header` for navigation and theme control.
- `.button` for primary actions.
- `.section` and `.section-title` for consistent content rhythm.
- `.project` for portfolio items.
- `.service-list` for capability rows.
- `.statement` for high-contrast messaging.
