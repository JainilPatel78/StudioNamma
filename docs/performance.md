# Performance notes

The current site intentionally avoids framework and dependency overhead. Visual artwork uses CSS gradients, and the page only loads one stylesheet and one small JavaScript file.

Future optimization can focus on image compression if photographic project assets are introduced.
