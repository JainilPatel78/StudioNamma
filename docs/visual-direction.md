# Visual direction

The design combines oversized editorial typography with restrained neutrals and one warm accent. Project artwork is generated in CSS so the demo stays lightweight and easy to run without a build pipeline.
