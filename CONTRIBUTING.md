# Contributing

Keep changes small and focused. Prefer semantic HTML, reusable CSS patterns, accessible interactions, and vanilla JavaScript unless a dependency is clearly justified.

Before opening a change, check the responsive layout and run through the manual checklist in `docs/testing.md`.
